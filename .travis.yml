sudo: false
language: python
matrix:
  include:
    - python: 2.7
    - language: generic
      os: osx
      env: PYTHON_VERSION=2.7.10

install:
  - ./.ci/travis/install.sh

script:
  - ./.ci/travis/test.sh

after_success:
  - coveralls
